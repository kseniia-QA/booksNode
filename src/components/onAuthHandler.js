const onAuthHandler = (event) => {
  event.preventDefault();
  const value = event;
  console.log(value);

  // if (value === 'mail@mail.com' && value2 === '1234') {
  //   // убрать модалку
  // } else {
  //   console.log('неа');
  // }
}

export default onAuthHandler;
